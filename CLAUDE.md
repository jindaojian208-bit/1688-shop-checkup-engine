# Claude 助手上下文 · 1688 店铺体检引擎

> 这份文件让任何 Claude 会话识别"新建 1688 店铺体检"触发词后，自动按 SOP 套模板初始化新店铺项目。

## ⛔ 必读 · 14 条红线（生成逻辑前必扫）

> 本文件已与红线准则**物理解耦**（engine-v3.0 起）。
> 进入店铺月度体检任务前，**必须先读** `红线准则/00_红线总表.md`（一页扫完 14 条）。

| 类别 | 文件 | 编号 |
|---|---|---|
| 总索引 | `红线准则/00_红线总表.md` | R-001 ~ R-014 |
| 方法论 | `红线准则/03_对比方法红线.md` | R-001 ~ R-004 |
| 平台合规 | `红线准则/02_平台合规红线.md` | R-005 ~ R-008 |
| 工程 | `红线准则/01_工程红线.md` | R-009 ~ R-014 |

**关键变更**（engine-v3.0 · 2026-04-26）：
- ⛔ **三梯度概念已废弃**：V2 用"普通 3-5 / 实力 2-3 / 超工 1-2"对比 → V3 改"自然首页 3-5 个"（详见 R-002）
- ⛔ **IP 授权红线写死**：禁止建议本店上 IP 词 SKU；同行 IP 词标 ⚠️侵权（详见 R-005）
- ⛔ **跨客户对比禁止**：金驰 vs 艺漫坊不行 / 任意两店不行（详见 R-001）

## 引擎身份

- 项目名：1688 店铺月度体检引擎
- 路径：`~/projects/_1688店铺体检引擎/`
- 定位：**引擎层 / 通用模板 / 与具体客户解耦**
- 已落地客户：金驰包装（`~/projects/jc-1688/`）/ 温州艺漫坊（`~/projects/ymf-1688体检/`）

## 触发词识别表

| 用户说 | Claude 立即做 |
|--------|--------------|
| **"新建 1688 店铺体检 \<名称\>"** | 进入"新店铺初始化 SOP"（见下方）|
| **"接入新 1688 店铺 \<名称\>"** | 同上 |
| **"复制 1688 体检引擎到 \<名称\>"** | 同上 |
| "跑 \<店铺\> 1688 月度体检" | 进入对应店铺项目，跑月度体检 SOP |
| "1688 引擎升级 / 模板优化 \<内容\>" | 修改 `~/projects/_1688店铺体检引擎/`，影响未来所有店铺 |

## 新店铺初始化 SOP（用户触发后 Claude 执行）

### Step 0 · 读红线（先于一切动作）

```bash
cat ~/projects/_1688店铺体检引擎/红线准则/00_红线总表.md
```

### Step 1 · 问 4 个问题

按顺序问，**用户答完才进 Step 2**：

1. 店铺品牌全名？（中文，如"杭州 XX 包装有限公司"）
2. 1688 二级域名？（如 `xxbao.1688.com` 或 `shop1234567890.1688.com`）
3. 主营类目结构？（按比例，如"塑料 60% / 纸盒 30% / 综合 10%"）
4. 商业化对待？（"会" / "可能" / "纯自用" / "不知道"）

> 第 4 题决定走 **完整路径** vs **轻量路径**（参考主 CLAUDE.md "新项目检查清单"）。

### Step 2 · 项目骨架建立

```bash
# 英文项目名规则：店铺主体英文/拼音 + "-1688体检"
# 例："杭州 XX 包装" → "hzxx-1688体检"
mkdir -p ~/projects/<英文名>/{01-诊断报告,02-数据快照,03-执行SOP,04-周度复盘,05-对接记录,06-人群投放配置,07-商品管理,08-参考资料,docs,scripts}
cd ~/projects/<英文名> && git init -b main
```

### Step 3 · 套模板（cp + 占位符替换）

```bash
# 复制单店铺 CLAUDE.md 模板
cp ~/projects/_1688店铺体检引擎/模板/单店铺_CLAUDE.md.template ./CLAUDE.md

# 复制报告模板
cp ~/projects/_1688店铺体检引擎/模板/报告模板/月度体检报告_V3模板.html ./01-诊断报告/

# 复制 7 份数据快照模板
cp ~/projects/_1688店铺体检引擎/模板/数据快照模板/*.md ./02-数据快照/

# 复制行动表模板
cp ~/projects/_1688店铺体检引擎/模板/行动表/月度行动表模板.md ./03-执行SOP/

# 占位符替换（用 sed -i '' 在 macOS）
find . -type f \( -name "*.md" -o -name "*.html" \) -exec sed -i '' \
  -e "s/{{店铺名}}/$店铺名/g" \
  -e "s/{{二级域名}}/$二级域名/g" \
  -e "s/{{主营类目}}/$主营类目/g" \
  -e "s/{{创建日期}}/$(date +%Y-%m-%d)/g" \
  -e "s/{{年框}}/未签 SKA/g" {} \;
```

### Step 4 · 注册 ccd 缩写

在主 CLAUDE.md（`~/.claude/projects/-Users-jindaojian/CLAUDE.md`）"已注册缩写"列表里加：

```
- `ccd <店铺缩写>` → ~/projects/<英文名>/（XX 1688 店铺月度体检）
```

### Step 5 · 写 memory 索引

在主 memory（`~/.claude/projects/-Users-jindaojian/memory/`）：

```bash
echo "- [<店铺名>](project_<英文名>.md) — 1688 店铺月度体检，~/projects/<英文名>/" >> MEMORY.md
```

并写 memory 文件 `project_<英文名>.md`。

### Step 6 · 首次 commit

```bash
git add .
git commit -m "feat: <店铺名> 1688 月度体检项目初始化（套引擎模板 V3）"
```

### Step 7 · 报告 owner

```
✅ 项目骨架就位
- 路径：~/projects/<英文名>/
- 进入：ccd <缩写>
- 触发首次月度体检：在项目内说"跑月度体检"
- 抓取 SOP：参考引擎方法论思维导图
- 红线索引：~/projects/_1688店铺体检引擎/红线准则/00_红线总表.md
```

## 月度体检 SOP（按需触发 · B-7 拍板）

详见 `方法论/1688店铺月度体检方法论_思维导图.html` 第四章「月度 SOP」。

**关键节奏**（owner 2026-04-26 拍板）：
- 触发：owner 说"跑 \<店铺\> 1688 月度体检"（不绑日期 / 不上 launchd）
- 桌面命名：`<店铺>1688月度体检报告_<月份>_V<主.次>.pdf`（删旧留新）
- 版本号：V 主.次双号（每月体检 +1 主，月内修正 +1 次）
- 主号发版打 git tag（如 `ymf-v2.0`）

## 模板更新流程（引擎升级）

当用户说"1688 引擎升级 / 模板优化"：

1. 在 `~/projects/_1688店铺体检引擎/` 内修改对应文件
2. 已落地的店铺**不自动同步**（避免破坏个性化）
3. 通过版本号追溯：每次升级 commit 标 `engine-v3.x → engine-v3.y`
4. 老店铺手动 cherry-pick 升级：用户决定要不要拉取

## 数据采集层（共用）

> 所有 1688 店铺数据源统一走以下入口：

| 类型 | 入口 | 备注 |
|------|------|------|
| 店铺前台 | `https://<二级域名>.1688.com/` | 公开 |
| 工厂档案 | 店铺前台 → 工厂档案 tab | 公开 |
| 工作台 | `work.1688.com` | 需登录 |
| 生意参谋 | `sycm.1688.com/portal/home.htm` | 需登录 |
| **生意参谋入店关键词** | `sycm.1688.com/ms/flow/word` | **客户搜什么词来 + 转化率**（V3 关键入口）|
| 竞店识别（流失 TOP10）| `sycm.1688.com/ms/compete/shopDiscern` | 需登录 |
| 竞品识别（同款 TOP10）| `sycm.1688.com/ms/compete/offerDiscern` | 需登录 |
| 新灯塔 5.0 | `work.1688.com/?_path_=sellerPro/lvyue/new-lighthouse-ai` | 需登录 |
| P4P | `p4p.1688.com/main.html` | 需登录 |

## 引擎自身的版本

| 版本 | 日期 | 变更 |
|------|------|------|
| engine-v1.0 | 2026-04-26 | 初版引擎落地，金驰包装首个落地客户验证完成 |
| engine-v2.0 | 2026-04-26 | IP 授权红线 + 同行三梯度对比 + 模块 6 矩阵 |
| **engine-v3.0** | 2026-04-26 | **三梯度删除（改自然首页 3-5 个）/ 红线物理解耦到 `红线准则/`（14 条 R-001~R-014）/ 数据源字段增补 5 项 / P0/P1/P2 改语义驱动 / 报告附录 4→3 / 节日倒推日历写死** |
